This plugin is developed based on the internal requirements of **zcy company**. It enables code navigation in projects through sidebar clicks, specifically targeting special annotations (primarily Extension, OpenAfter, OpenBefore, and OpenReplace annotations). The core functionality allows clicking on annotations like OpenAfter to jump to the corresponding code location marked with an Extension annotation that shares the same value parameter.
